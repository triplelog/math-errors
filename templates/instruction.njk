<div class="flex w-full">
	<div class="w-auto inline-block object-center border rounded">
		<div class="w-full px-4 py-4">
		{{ node.getAttribute("cards").in | safe }}
		</div>
		{% for constraint in node.getAttribute("cards").constraints %}
		<hr>
		<div class="w-full bg-red-100 px-2">
			{{ constraint | safe }}
		</div>
		{% endfor %}
	</div>
	<div class="w-auto inline-block object-center mx-2 py-4">
		---->
	</div>
	<div class="w-auto inline-block">
		<div class="w-full object-center px-4 py-4 border rounded">
		{{ node.getAttribute("cards").out | safe }}
		</div>
	</div>
</div>